(define (a-plus-abs-b a b)
  ((if (> b 0) + -) a b))

В зависимости от значения условного выражения выполняет одну из операций (в данном случае + или -) над последующим набором данных (в данном случае a и b)
